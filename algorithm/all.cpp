#include<iostream>
#include<stack>
#include<queue>

using namespace std;

int main(){
    //기억해야할 것
    // 1. dfs
    // dfs는 backtracking이 사용되고, stack과 재귀함수로 구현할 수 있다.
    // 방문 처리 하는 시점은 pop 할 때

    // 2. bfs
    // bfs는 순차적으로 방문하므로, 최단경로 찾을 때 쓸 수 있다.
    // 방문 처리하는 시점은 push할 때

    // 3. backtracking
    // 방문처리를 되돌리는 이유는, dfs랑 달리 다시 사용될 수 있기 때문이다.
}